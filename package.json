{
  "name": "fallback-chain-js-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.3",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
