{
  "name": "@khalidsaidi/fallback-chain-js",
  "version": "0.2.0",
  "description": "Tiny fallback chains for JS/TS — try providers until one succeeds.",
  "type": "module",
  "main": "./dist/index.cjs",
  "types": "./dist/index.d.cts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "llms.txt",
    "llms-full.txt"
  ],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no lint configured\"",
    "test:node": "npm run build && node --test",
    "test:bun": "npm run build && bun test",
    "test:workers": "npm run build && vitest run --config vitest.config.ts",
    "test": "npm run test:node && npm run test:workers",
    "prepublishOnly": "npm run build && npm run typecheck && npm test"
  },
  "keywords": [
    "fallback",
    "failover",
    "resilience",
    "control-flow",
    "abortsignal",
    "timeout",
    "workers",
    "bun",
    "typescript",
    "llm",
    "ai"
  ],
  "license": "MIT",
  "sideEffects": false,
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/khalidsaidi/fallback-chain-js.git",
    "directory": "packages/fallback-chain-js"
  },
  "bugs": {
    "url": "https://github.com/khalidsaidi/fallback-chain-js/issues"
  },
  "homepage": "https://github.com/khalidsaidi/fallback-chain-js#readme",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.7",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "~3.2.7"
  }
}
